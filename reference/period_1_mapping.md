# Column Mapping for Period 1 (2002-2012)

Period 1 raw columns mapped to unified 20-column structure. Periods 1-2
omit dimension columns and score history columns (set as NA). Named in
target column order.

## Usage

``` r
period_1_mapping
```
